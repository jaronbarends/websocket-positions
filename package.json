{
  "name": "websocket-positions",
  "version": "0.0.1",
  "description": "calculate device positions",
  "dependencies": {
    "express": "^4.10.2",
    "socket.io": "^1.7.4"
  },
  "engines": {
    "node": "0.12.x"
  },
  "scripts": {
    "preinstall": "npm install --save socket.io --msvs_version=2013",
    "start": "node socket-position-server.js"
  }
}
